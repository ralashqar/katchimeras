{
  "background": {
    "_comment": "Today background framing. scale >= 1 zooms the cover-fit image; offsetY in dp shifts it (negative = up). The code clamps offsetY so the image always fills the frame: |offsetY| <= (scale - 1) * screenHeight / 2.",
    "scale": 1.08,
    "offsetY": -20
  }
}
