/**
 * Islands whose revealed art is bundled in the scene's art table
 * (`components/katchadeck/world/mossprout-hex-neighborhood-scene.ts`),
 * listed here without the images so the content-pack validator can know a
 * bundled pack's island needs no art entry. Keep the two in step.
 */
export const BUNDLED_NATURE_ISLAND_ART_IDS: readonly string[] = ['seed-nursery', 'bloom-garden', 'pond-sanctuary', 'orchard-grove', 'ancient-tree-grove', 'wildgrowth-grove', 'wanderling-trail', 'rush-track'];
