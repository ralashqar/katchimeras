# Silver breakable shell

Built-in imagegen, 2026-09-07. Replaces the blue shield-benefit emblem with an opaque silver-grey obstacle cell. Source: `armour-silver.png`; packaged by `package-shell-art.py` with the same 126px square extent and 1px gutter as regular cells.

Direction: silver grey cushiony rounded square made of eggshell, embossed circular rim, blank recessed gray center for the live dark number, sparse grey shell spots, front-on camera, no shield emblem or glow. True alpha outside the solid tile. Keep the existing chip/entrance/break-away mechanics; no new power-up.

Runtime text uses the host's hero font with charcoal ink. The number retains the existing drops-remaining semantics (including the final filling placement).
