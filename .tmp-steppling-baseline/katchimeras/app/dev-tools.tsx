// The emergency FTUE escape must push a new root-stack route so the active
// tutorial screen does not need to be removed. Reuse the complete Developer
// Tools page rather than sending the player to one of its subsections.
export { default } from './(tabs)/explore';
